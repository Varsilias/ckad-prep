apiVersion: v1
kind: Service
metadata:
  labels:
    app: go-app
  name: go-app
spec:
  ports:
  - name: go-app
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: go-app
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: go-app
  name: go-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: go-app
  template:
    metadata:
      labels:
        app: go-app
    spec:
      containers:
      - image: varsilias/go-ms:normal
        name: go-ms
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi


